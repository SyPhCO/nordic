{# {% extends 'base.html.twig' %}

{% block title %}Qui suis-je ? 
{% endblock %}

{% block content %}
	{% for team in teams %}
<hr>
<h1 class="text-center">{{ team.name}}</h1>
<div class="row"> 
	<div class="col-lg-2 col-sm-12 container d-flex flex-column justify-content-center text-center">
		<div class="mt-5 actiGal">
			<img src="/uploads/{{ team.gallery }}" alt="{{ team.name }}" class="img-fluid">
		</div>
		<div class="mt-5 actiGal">
			<img src="/uploads/{{ team.gallery2 }}" alt="{{ team.name }}" class="img-fluid">
		</div>
		<div class="mt-5 actiGal">
			<img src="/uploads/{{ team.gallery3 }}" alt="{{ team.name }}" class="img-fluid">
		</div>

	</div>

	<div class="col-lg-8 col-sm-12 mt-5">
		<div class="col-md-8 mt-5">
			<img src="/uploads/{{ team.illustration }}" alt="{{ team.name }}" class="img-fluid">
		</div>

				
			<hr>
			<p>{{ team.description|nl2br|raw }}
			</p>

	</div>
	<div class="col-lg-2 col-sm-12 container d-flex flex-column justify-content-center text-center">
		<div class=" mt-5 actiGal">
			<img src="/uploads/{{ team.gallery4 }}" alt="{{ team.name }}" class="img-fluid">
		</div>
		<div class="mt-5 actiGal">
			<img src="/uploads/{{ team.gallery5 }}" alt="{{ team.name }}" class="img-fluid">
		</div>
		<div class="mt-5 actiGal">
			<img src="/uploads/{{ team.gallery6 }}" alt="{{ team.name }}" class="img-fluid">
		</div>
	</div>
</div>


	{% endfor %}


{% endblock %} #}
{% extends 'base.html.twig' %}

{% block title %}Qui suis-je ?{% endblock %}

{% block content %}
{% for team in teams %}
	<h1 class="text-center">{{ team.name}}</h1>
<div class="row mt-5">

    <div class="row d-flex flex-row justify-content-center col-lg-8">
			<img src="/uploads/{{ team.illustration }}" alt="{{ team.name }}" class="img-fluid">
		
			<hr>
			<p>{{ team.description|nl2br|raw }}
			</p>
	</div>

        <div class="col-lg-2 col-sm-6 col-6 text-center d-flex flex-column ">
            <div class="mt-5 ">
                <img src="/uploads/{{ team.gallery }}" alt="{{ team.name }}" class="img-fluid rounded">
            </div>
            <div class="mt-5">
                <img src="/uploads/{{ team.gallery2 }}" alt="{{ team.name }}" class="img-fluid rounded">
            </div>
            <div class="mt-5">
                <img src="/uploads/{{ team.gallery3 }}" alt="{{ team.name }}" class="img-fluid rounded">
            </div>
        </div>

        <div class="col-lg-2 col-sm-6 col-6 text-center d-flex flex-column">
            <div class="mt-5">
                <img src="/uploads/{{ team.gallery4 }}" alt="{{ team.name }}" class="img-fluid rounded">
            </div>
            <div class="mt-5">
                <img src="/uploads/{{ team.gallery5 }}" alt="{{ team.name }}" class="img-fluid rounded">
            </div>
            <div class="mt-5">
                <img src="/uploads/{{ team.gallery6 }}" alt="{{ team.name }}" class="img-fluid rounded">
            </div>
        </div>

</div>	
<hr>
{% endfor %}

{% endblock %}