<div class="product-item text-center">
                        <a href="{{ path('app_product', { 'slug' : product.slug }) }}"><img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="figure-img img-fluid img-thumbnail max-width:200px"></a>
                        <h4>{{ product.name }}</h4>
                        <span class="product-subtitle">{{ product.subtitle }}</span>
                        <span class="product-price font-weight-bold">A partir de: {{ (product.price / 100)|number_format(2, ',', '.') }} €</span>
                    </div>