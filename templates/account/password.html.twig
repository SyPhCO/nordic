{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe{% endblock %}

{% block content %}
<h1> modifier votre mon de passe </h1>
<a href="{{ path('app_account')}}" class="btn btn-outline-danger btn-sm">Retour</a><br>
<hr>
{% if notification %}
<div class="alert alert-info">{{ notification }} </div>
{% endif %}
{{ form(form) }}
<hr>
{% endblock %}
