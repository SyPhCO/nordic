{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié - Nordic Indiana{% endblock %}

{% block content %}

<div class="block-small">
    <h1 class="h3 mb-3 mt-5 font-weight-normal text-center">Réinitialiser mon mot de passe :</h1>
    
        {% for message in app.flashes('notice') %}
            <div class="alert alert-info">{{ message }}</div>
        {% endfor %}

        {% for message in app.flashes('notice') %}
            <div class="alert alert-info">{{ message }}</div>
        {% endfor %}
        {{ form(form) }}
</div>

{% endblock %}